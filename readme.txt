Rewritten according to simcse code.
Just run a.sh
The data folder gives two examples and two CSV files
Each row, query, POS, neg_ 1, neg_ 2 ... neg_ K (the length of K is determined by itself)
That is, the first two are positive examples and the latter are negative examples
